# 开发 Agent 工作约定

适用整个仓库。用户当前明确请求优先；历史交接、验收记录和业务 SKILL.md 是背景材料，不自动构成新的执行授权。

## 当前需求与模型协作

- 用户2026-09-09明确：后续开发发生冲突，以本次提供的四份V2文件为准，入口为 `docs/specs/V2.0-文件索引.md`。旧规格、提示词、测试和交接记录不得覆盖新规则；原始证据保留。
- 四文件已明确的变更直接对齐；仅对彼此矛盾、留白或新增业务决策与用户确认。整张蓝图不等于本轮全部开发批准。
- 当前复核与后续计划：`HANDOFF.md`；模型协作：`docs/standards/MODEL_WORKFLOW.md`。高风险根因与关键验收由高能力模型参与，经济模型先通过同要求小样本验证。
- 新任务提供已核验事实、待验证假设、历史方案及代码基线，避免把旧失败或完成标签直接当新分析前提；收尾更新HANDOFF当前摘要和证据。

## 开始任务

1. 先读 `docs/standards/STANDARDS.md` 和 `docs/standards/REUSE.md`。
2. 查看 `git status --short`；保留其他会话的未提交文件。按当前任务阅读 `HANDOFF.md`、相关任务卡和 Git 历史，不自动执行历史待办。
3. 修改前用 `rg` 搜索业务词、现有函数/服务、调用方和对应测试；先读实现再决定是否新增。
4. 简短说明本次将复用哪些路径；确需新增时，说明已查过的候选和无法满足需求的具体原因。已获授权的工作直接推进，不为例行实现选择反复询问。

## 复用与新增

- 顺序：调用现有能力 → 在兼容现有调用方的前提下扩展 → 确有职责差异才新增。
- HTTP、权限、错误处理、脱敏、AI 调度、数据库连接、测试辅助优先用索引中的入口。
- 不按名字相似机械合并：先核对权限、输入输出、事务、空值、时区、金额单位、幂等与失败语义。
- 一次性调试脚本使用系统临时目录，结束清理；不要把临时脚本变成新的公共工具。
- 经常复跑的检查/评测优先扩展已有入口；正式新增脚本时写明调用方式、是否写库/联网、测试位置，并更新复用索引。
- 验收目录中的历史脚本/快照是证据；不能未经核对就当当前实现使用，也不要擅自清理证据。
- 小范围修复保持范围清晰；发现无关重构点先记录，避免顺手扩大修改。

## 防止重复犯错

- 修 Bug 先复现，再修根因；在已有测试中补能复现该问题的回归用例，避免仅测试实现细节。
- 先查 `HANDOFF.md` §8 和相关回归测试。尤其注意 HNSW 索引误删、AI 请求超时、鉴权文件 URL、测试数据污染。
- Prisma 生成迁移后先读 SQL，再运行 `node scripts/governance.mjs`；确认无误才应用。不要修改已应用的历史迁移。
- 修改大型 Vue 组件时优先按功能提取已有可复用部分；新增组件 ≤300 物理行。旧超长组件不能超过 `scripts/governance-baseline.json` 的限额；拆分后同步下调/删除对应基线，禁止调高基线或新增豁免使门禁通过。
- 迁移扫描只覆盖显式 DROP INDEX；动态/过程体 SQL、DROP TABLE/CASCADE 等仍须人工审查。检查通过不代表可以省略迁移审查。

## 验证与交付

- 快速治理检查：`node --test scripts/governance.test.mjs && node scripts/governance.mjs`（只读源码，不联网、不写库）。
- 按改动运行针对性测试；提交/验收按原规范运行 `bash scripts/ci.sh`。后端测试会迁移测试库，不是只读检查；使用现有测试库隔离配置。
- 改业务 AI skill/提示词时遵守 A08/A09 的评测与发布规则，不把开发技能当作运行时技能发布。
- 交付说明实际复用/新增的能力、测试命令与结果、未验证项；不能把历史测试结果当本轮结果。
- 不新增运行时依赖来完成普通治理；优先扩展现有 CI、ESLint、Vitest 和 Node 内置检查。
