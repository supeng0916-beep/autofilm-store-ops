# 验证记录

日期：2026-09-11。对象是独立目录中的招聘展示候选。本页记录本次实际执行结果；程序测试不等于真实模型效果或业务收益。

## 环境与范围

- 使用独立代码副本及新初始化的 PostgreSQL 16 / pgvector 实例。数据库仅监听本机独立端口，测试库与上传目录单独配置，没有使用原项目数据库。
- 复用独立导出副本已安装的锁定依赖；没有修改依赖锁文件或迁移 SQL。
- 修改涉及展示文档、隐私内容清理和合成人员种子。同步了引用人员种子的测试夹具，权限、冲突、技能校验与返回值断言保持原意。
- 五张截图逐张目检；图片字节未变，人物、任务、金额与指标均为合成展示内容。

## 本轮结果

| 检查 | 结果与范围 |
| --- | --- |
| 治理测试与源码检查 | 8/8 通过，治理检查通过 |
| 后端 Lint / 类型检查 | 完整 CI 中通过 |
| 后端全量测试 | 97 个文件，1051/1053 通过，2 项失败，见下文 |
| 后端相关集成复跑 | 8 个文件，90/90 通过：团队、预约、施工、订单确认、闭环回放、上门服务，以及全量执行出现异常的知识 CRUD / 客资分配测试 |
| 后端早期专项检查 | 5 个文件，35/35 通过：输入归一化、合成评测数据、MCP 计数、假 WebSocket 网关、空知识种子；此批采用不连接数据库的临时测试配置 |
| 前端专项检查 | AftercareView / AssetsView 共 27/27 通过 |
| 前端完整检查 | 类型检查、Lint、全量测试及生产构建命令顺序执行，退出 0；测试期间有本机模拟请求的 ECONNREFUSED 日志，构建有既有动态导入分包提示 |
| 密钥扫描 | bash scripts/secret-scan.sh 通过；模式检查不能代替客户资料人工复核 |
| 差异检查 | git diff --cached --check 通过；依赖锁文件、迁移 SQL 和五张截图未修改 |
| 本地文档预览 | 桌面与窄屏目检，首页、架构与图集可显示；检查时无页面 JS 错误，窄屏页面无整体横向溢出 |

可核对的脱敏输出摘录见 [验证证据](portfolio/verification/README.md)。完整 CI 首次发现部分旧种子名称引用，修正合成夹具后重新执行得到上表结果。

完整 CI 使用显式隔离配置运行：

```bash
WG_DATABASE_URL=postgresql://recruiter_preview@127.0.0.1:55447/autofilm_recruiter_test \
WG_TEST_DATABASE_URL=postgresql://recruiter_preview@127.0.0.1:55447/autofilm_recruiter_test \
WG_UPLOAD_DIR=/tmp/autofilm-recruiter-test-uploads \
WG_JWT_SECRET=test-only-portfolio-jwt-secret-32chars \
WG_HEARTBEAT=on WG_INDUSTRY_DAILY=on \
bash scripts/ci.sh
```

这里的通知开关服务于假发送器与假网关测试，不表示启用真实通知。日常启动默认关闭。

## 未通过项与未验证范围

`bash scripts/ci.sh` 退出 1，在后端测试处停止。因此前端命令和密钥扫描另行执行，不能称完整 CI 全绿。

- `knowledge-search.e2e.spec.ts`：“存在相关条目时返回检索结果”失败。当前未配置 Embedding，返回结果缺少断言所需的 itemId；保留检索行为与原断言。
- `knowledge.e2e.spec.ts`：“不存在条目返回 404”在本次全量执行中收到不能解析为 JSON 的 `No mapping` 响应。随后相关 8 个集成文件复跑全部通过，包括该用例；尚未定位该间歇异常的根因，不用单项通过覆盖全量失败记录。

脱敏运行时技能没有接入真实网关进行新一轮模型评测，不能描述为已通过模型发布验收。本轮未执行生产部署、备份恢复或 Docker 安装路线。启动指南依据 Makefile、脚本与配置核对，后端集成测试在独立数据库运行；不把此前启动记录当成本轮验证。

## 发布状态

作者随后要求取消独立展示网站，现改为 README 与仓库 Markdown 直接展示，原本的文档预览服务已停止。上述浏览器检查是此前预览阶段的记录。内容集中调整仅涉及 Markdown，没有改动业务源码、技能、测试、配置或截图，也未重复运行数据库测试与模型评测。

作者已授权将清理后的项目推送并保持公开。本次发布采用不继承输入历史的独立根快照，复查文件、截图、配置模板与待公开 Git 对象；旧仓库另名私有归档。README 提供正式克隆入口，公开范围见 [隐私与发布范围](PRIVACY_EXPORT.md)。此次发布准备只更新展示文档和发布状态，前述源码测试的结果与限制保持不变。
